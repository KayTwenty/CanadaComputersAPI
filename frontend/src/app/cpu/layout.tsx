export const metadata = {
    title: 'All CPU Deals — CCDeals',
    description: 'Browse every processor deal from Canada Computers, sorted and filtered by store location.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
