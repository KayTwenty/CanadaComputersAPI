export const metadata = {
    title: 'All Memory Deals — CCDeals',
    description: 'Browse every RAM and memory kit deal from Canada Computers, sorted and filtered by store location.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
